
  # Mid Money

  ## Running the code

  Run `npm i` to install the dependencies.

  For Supabase integration, copy `.env.example` to `.env`

  If these variables are missing, the app runs in local-only mode.

  Run `npm run dev` to start the development server.
  